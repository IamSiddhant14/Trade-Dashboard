services:
  - type: web
    name: trade-dashboard-backend
    runtime: node
    plan: free
    buildCommand: npm install && npm run setup:backend
    startCommand: node render-backend.cjs
    healthCheckPath: /health
